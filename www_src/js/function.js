$(document).ready(function () {

    var newheight = 0 //impostiamo un'altezza iniziale

    var padding = ($(".box1").css("padding")).split('px')
    //estraggo, se c'è, il padding della classe, dividendolo dall'unità di misura 'px'

    $(".box1").each(function () { // Per ogni div con la classe .single_box

        if ($(this).height() > newheight)
        // se l'altezza di questo div è maggiore dell'attuale
        //valore di NEWHEIGHT, reimposto il valore di questa variabile

        {
            newheight = $(this).height()
        }
        ;

        //aggiungo all'altezza del più alto il padding estratto,
        //moltiplicato per 2 , per avere un leggero margine in piu


    });
    var compHeight = newheight + 10 + "px"

    $(".box1").css("height", compHeight); //imposto al volo il valore 'height' nel CSS, con il nuovo valore trovato.
});

$(document).ready(function () {

    var newheight = 0 //impostiamo un'altezza iniziale

    var padding = ($(".box2").css("padding")).split('px')
    //estraggo, se c'è, il padding della classe, dividendolo dall'unità di misura 'px'

    $(".box2").each(function () { // Per ogni div con la classe .single_box

        if ($(this).height() > newheight)
        // se l'altezza di questo div è maggiore dell'attuale
        //valore di NEWHEIGHT, reimposto il valore di questa variabile

        {
            newheight = $(this).height()
        }
        ;

        //aggiungo all'altezza del più alto il padding estratto,
        //moltiplicato per 2 , per avere un leggero margine in piu


    });
    var compHeight = newheight + 10 + "px"

    $(".box2").css("height", compHeight); //imposto al volo il valore 'height' nel CSS, con il nuovo valore trovato.
});

$(document).ready(function () {

    var newheight = 0 //impostiamo un'altezza iniziale

    var padding = ($(".box3").css("padding")).split('px')
    //estraggo, se c'è, il padding della classe, dividendolo dall'unità di misura 'px'

    $(".box3").each(function () { // Per ogni div con la classe .single_box

        if ($(this).height() > newheight)
        // se l'altezza di questo div è maggiore dell'attuale
        //valore di NEWHEIGHT, reimposto il valore di questa variabile

        {
            newheight = $(this).height()
        }
        ;

        //aggiungo all'altezza del più alto il padding estratto,
        //moltiplicato per 2 , per avere un leggero margine in piu


    });
    var compHeight = newheight + 10 + "px"

    $(".box3").css("height", compHeight); //imposto al volo il valore 'height' nel CSS, con il nuovo valore trovato.
});

$(document).ready(function () {

    var newheight = 0 //impostiamo un'altezza iniziale

    var padding = ($(".box4").css("padding")).split('px')
    //estraggo, se c'è, il padding della classe, dividendolo dall'unità di misura 'px'

    $(".box4").each(function () { // Per ogni div con la classe .single_box

        if ($(this).height() > newheight)
        // se l'altezza di questo div è maggiore dell'attuale
        //valore di NEWHEIGHT, reimposto il valore di questa variabile

        {
            newheight = $(this).height()
        }
        ;

        //aggiungo all'altezza del più alto il padding estratto,
        //moltiplicato per 2 , per avere un leggero margine in piu


    });
    var compHeight = newheight + 10 + "px"

    $(".box4").css("height", compHeight); //imposto al volo il valore 'height' nel CSS, con il nuovo valore trovato.
});

$(document).ready(function () {

    var newheight = 0 //impostiamo un'altezza iniziale

    var padding = ($(".box5").css("padding")).split('px')
    //estraggo, se c'è, il padding della classe, dividendolo dall'unità di misura 'px'

    $(".box5").each(function () { // Per ogni div con la classe .single_box

        if ($(this).height() > newheight)
        // se l'altezza di questo div è maggiore dell'attuale
        //valore di NEWHEIGHT, reimposto il valore di questa variabile

        {
            newheight = $(this).height()
        }
        ;

        //aggiungo all'altezza del più alto il padding estratto,
        //moltiplicato per 2 , per avere un leggero margine in piu


    });
    var compHeight = newheight + 10 + "px"

    $(".box5").css("height", compHeight); //imposto al volo il valore 'height' nel CSS, con il nuovo valore trovato.
});

$(document).ready(function () {

    var newheight = 0 //impostiamo un'altezza iniziale

    var padding = ($(".box6").css("padding")).split('px')
    //estraggo, se c'è, il padding della classe, dividendolo dall'unità di misura 'px'

    $(".box6").each(function () { // Per ogni div con la classe .single_box

        if ($(this).height() > newheight)
        // se l'altezza di questo div è maggiore dell'attuale
        //valore di NEWHEIGHT, reimposto il valore di questa variabile

        {
            newheight = $(this).height()
        }
        ;

        //aggiungo all'altezza del più alto il padding estratto,
        //moltiplicato per 2 , per avere un leggero margine in piu


    });
    var compHeight = newheight + 10 + "px"

    $(".box6").css("height", compHeight); //imposto al volo il valore 'height' nel CSS, con il nuovo valore trovato.
});